function main() -> int
{
	matrix : x(2, 2);
	x = [1, 2 | 3, 4];
	int : i = 0;
	int : j = 0;
	string : tmp;
	for(i = 0; i < 2; i = i + 1)
	{
		j = 0;
		for(j = 0; j < 2; j = j +1)
		{
			tmp = "mat[" + StrParse(i);
			tmp = tmp + "][";
			tmp = tmp + StrParse(j);
			tmp = tmp + "]: ";
			tmp = tmp + StrParsef(x[i][j]);
			prints(tmp);
		}
	}
	return 0;
}