function main() -> int
{
        image: a = read("./images/face1.jpg");
        
        image: output;
        ##output = adjust_contrast(a, 1.1);
        ##output = to_gray(a);
		##output = adjust_brightness(a, 20.0);
		output = adjust_saturation(a, 1.1);
	    
	    save("test.out.jpg", output);

        return 0;
}
